name: OneManager2 Auto Restart

on:
  push:
    branches: master
  watch:
    types: started
  schedule:
    - cron: 30 22 * * *

jobs:
  IBM-Cloud-CF-APP-Restart:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install CF CLI
      run: |
        curl -L "https://packages.cloudfoundry.org/stable?release=linux64-binary&source=github&version=v6" | tar -zx
        sudo mv cf /usr/local/bin
      
    - name: Login CF
      run: cf l -a https://api.${{ secrets.OneManager2_RGN }}.cf.cloud.ibm.com login -u ${{ secrets.OneManager2_MAIL }} -p ${{ secrets.OneManager2_PWD }}
      
    - name: Restart CF APP
      run: cf rs ${{ secrets.OneManager2_CFNAME }}
